#include<stdio.h>
int main(){
	int i=2, n;
	float ac=1;
	printf("Ingrese n: ");
	scanf("%d",&n);
	for (i=2; i<=2*n; i += 2){
		ac = ac * i/(i-1);
		ac = ac * i/(i+1);
	}
	printf("el valor aproxdimado de pi es: %.4f\n", ac*2);
	
	return 0;
}
